- @users.each do |group|
  .users-level-block
    %center
      - group.last.each do |user|
        = link_to admin_activity_path(user), method: :put, data: { confirm: 'Are you sure?' }, remote: true do
          .user-badge{ id: "user_#{user.id}", class: "#{ user.online? ? 'online' : 'offline' }"}
            %div
              = user.full_name
            %div
              = user.rank
            %div
              = user.position
  %hr
